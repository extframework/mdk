[repositories]
local = true

[parents]
minecraft = { group = "com.kaolinmc.core", version = "1.0.7-BETA", use = "central" }
init-hook = { isBuild = true }
resource-loader = { isBuild = true }
keybind = { isBuild = true }
