[repositories]
central = true

[parents]
minecraft = { group = "dev.extframework.core", version = "1.0.6-BETA", use = "central" }
init-hook = { isBuild = true }
resource-loader = { isBuild = true }
keybind = { isBuild = true }
